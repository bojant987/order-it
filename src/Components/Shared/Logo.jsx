import React from 'react';
import PropTypes from 'prop-types';

const Logo = props => {
    return (
        <div className="Logo h-textCenter h-marginB--sm">
            <span className="Logo__inner">Order It!</span>
        </div>
    );
};

export default Logo;